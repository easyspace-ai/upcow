// Package template provides a **compile-able** strategy skeleton.
//
// It is intentionally NOT registered via init(), so it won't affect production behavior.
// Copy this package to create new strategies following the project's standard patterns.
package updown
