# 总成本保护修复

**问题**: 三单的总成本都是 101c（Entry + Hedge > 100c），导致无论哪边 win 都会亏损

**修复时间**: 2025-12-25

## 📊 问题分析

### 交易记录

| 交易 | Entry | Hedge | 总成本 | 状态 |
|------|-------|-------|--------|------|
| #1 | 60c | 41c | **101c** ❌ | 亏损 |
| #2 | 63c | 38c | **101c** ❌ | 亏损 |
| #3 | 62c | 39c | **101c** ❌ | 亏损 |

### 问题原因

1. **总成本保护阈值过高**: 原代码中总成本保护阈值是 105c，而这三单都是 101c，所以没有被拒绝
2. **有效价格计算**: 使用镜像订单簿计算有效价格时，可能导致总成本略高于 100c
3. **价格滑点**: 订单簿价格在获取和下单之间可能发生变化

## 🔧 修复方案

### 1. 降低总成本保护阈值

**修复前**:
```go
// 如果总成本过高（> $1.05），说明价格可能有问题，拒绝下单
if totalCostCents > 105 {
    log.Warnf("⚠️ [%s] 价格滑点保护触发: 总成本过高 (%dc > 105c, ...)", ...)
    return nil
}
```

**修复后**:
```go
// 如果总成本 > $1.00，说明无论哪边 win 都会亏损，拒绝下单
if totalCostCents > 100 {
    log.Warnf("⚠️ [%s] 价格滑点保护触发: 总成本超过 $1.00 (%dc > 100c, ...) - 拒绝下单以避免亏损", ...)
    return nil
}

// 如果总成本过高（> $0.98），记录警告（但仍允许下单，因为可能是合理的价差）
if totalCostCents > 98 {
    log.Warnf("⚠️ [%s] 总成本较高: %dc (...) - 注意：如果总成本 > 100c 会拒绝下单", ...)
}
```

### 2. 添加警告日志

- 总成本 > 98c: 记录警告，但仍允许下单
- 总成本 > 100c: 拒绝下单，避免亏损

## 📋 影响

### 修复前
- ❌ 总成本 101c 的订单会被接受
- ❌ 无论哪边 win 都会亏损
- ❌ 没有足够的保护

### 修复后
- ✅ 总成本 > 100c 的订单会被拒绝
- ✅ 避免亏损订单
- ✅ 总成本 > 98c 时记录警告，提醒注意

## 🔍 为什么总成本会是 101c？

可能的原因：

1. **订单簿价差**: 
   - Entry ask = 60c, Entry bid = 59c (价差 1c)
   - Hedge ask = 41c, Hedge bid = 40c (价差 1c)
   - 总价差 = 2c，但实际总成本可能因为镜像订单簿计算而略高

2. **镜像订单簿计算**:
   ```go
   effectiveBuyEntry := entryAskDec
   if 1-hedgeBidDec < effectiveBuyEntry {
       effectiveBuyEntry = 1 - hedgeBidDec
   }
   
   effectiveBuyHedge := hedgeAskDec
   if 1-entryBidDec < effectiveBuyHedge {
       effectiveBuyHedge = 1 - entryBidDec
   }
   ```
   - 如果 `1-hedgeBidDec` 或 `1-entryBidDec` 更优，会使用镜像价格
   - 但两个价格相加可能略高于 100c

3. **价格滑点**: 订单簿价格在获取和下单之间可能发生变化

## 💡 建议

1. **监控总成本**: 在日志中记录总成本，便于分析
2. **优化价格计算**: 考虑使用更严格的价格计算逻辑，确保总成本 <= 100c
3. **实盘测试**: 在实盘模式下测试，观察总成本保护是否正常工作

## ✅ 验证

修复后，如果总成本 > 100c，订单会被拒绝，日志会显示：
```
⚠️ [velocityfollow] 价格滑点保护触发: 总成本超过 $1.00 (101c > 100c, entry=60c hedge=41c, 总成本=101c, source=...) - 拒绝下单以避免亏损
```

